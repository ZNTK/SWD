<Window x:Class="SWD.Charts.EDChartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:local="clr-namespace:SWD.Charts"
        mc:Ignorable="d"
        Title="ChartWindow" Height="800" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <lvc:CartesianChart Name="cartesianChart" Grid.Row="0" Margin="20">
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Name="axisY" Title=""></lvc:Axis>
            </lvc:CartesianChart.AxisY>
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Name="axisX" Title="" ></lvc:Axis>
            </lvc:CartesianChart.AxisX>
        </lvc:CartesianChart>
        <Label Name="labelDlugosc" Grid.Row="1" Content="D: " HorizontalAlignment="Center"/>
        <Button Name="buttonClose" Grid.Row="3" Height="30" Content="Zamknij" Click="buttonClose_Click"></Button>
    </Grid>
</Window>
